#include <stdio.h>

int main()

{
    double first_term=71, second_term=83, final_term=85, total_marks;
    total_marks= first_term * .25 + second_term * .25 + final_term * .5;
    printf("Total marks= %lf", total_marks);

}
